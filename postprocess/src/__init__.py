"""Compatibility package for legacy imports.

This module exists so historical pickled models that reference the old
`src.va_regression` path can still be deserialized after extracting
PostPreprocess as a standalone toolkit.
"""
